import { ConfirmedTripClient } from "./ConfirmedTripClient";

/**
 * Confirmed / active / completed trip detail for a booking the user participates in.
 */
export default function ConfirmedTripPage() {
  return <ConfirmedTripClient />;
}
